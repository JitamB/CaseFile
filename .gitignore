# §41.2 — what is committed, and what is not.
# llm_cache/ and data/corpus/ are committed on purpose.
# Recorded model responses are what make CI offline and the demo reproducible.

initials/

.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.DS_Store

.env

data/raw/
data/manifest.json
*.duckdb
*.duckdb.wal
ui/node_modules/
ui/dist/
